kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: scanner
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30080
        hostPort: 3000
        protocol: TCP
      - containerPort: 30500
        hostPort: 5000
        protocol: TCP

